//setting assistant editor 
// using certain function to determine what data to show

/*
let x = -10

if x > 0 {
    println(x)
} else {
    println(x)
}

//if = hey, check to see whether something is true

let z = -5

if z > 0 {
    println(y)
} else {
    println(x)
}

if x > 0 {
    println("Yes, \(x) is greather than zero")
} else {
    println("No. \(x) is less than zero")
}
*/

// if user.loggedIN == true { = exampel of how real app operates
// what is x = 0 -> we run into the problem what we have not envisioned

/*
let y = -10


if y > 0 {
    println ("Yes, /(y) is greater than zero")
} else if y == 0 {
    println ("WOOHOO, \(y) is equalt to Zero!")
} else {
    println ("No. \(y) is less than zero")
}
*/

//while {}  -> must be a variable 

//var z = 0
//while z < 100  {
//    println ("Hey! \(z)")
//    ++z
//}

// make highlighted comment -> command + /


//ForinLoop

for i in 1...100 {
    println ("Print the current iteration: \(i)")
}

for toyota in 1..<100 {
println ("Print the current iteration: \(toyota)")
}

//var toyota = 100 not changing anything -> separate variable

// i = dummy variable (i can be anything but what programmers usually use)

// "&&" sign both have to b ture 


